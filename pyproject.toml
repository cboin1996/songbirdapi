[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "songbirdapi"
authors = [
    {name = "Christian Boin"},
]
version = "0.0.4"
description = "Music download api"
readme = "README.md"
requires-python = ">=3.9"
